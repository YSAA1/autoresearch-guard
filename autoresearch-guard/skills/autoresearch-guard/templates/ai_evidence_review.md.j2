# AI 证据审查

## 证据摘要

TBD by AI（在运行 `arx_audit.py` 后填写）。

## 结论与证据

每个 claim 必须使用最弱可支持等级。`状态` 只能表达 `supported`、`unsupported` 或 `prohibited`。

| claim_id | 结论 | 等级 | 证据 | 状态 |
| --- | --- | --- | --- | --- |

## 失败归因

TBD by AI（待填写）。请勿让脚本填写本节。

## 下一步动作

TBD by AI（待 AI 与人工门禁确认后填写）。
