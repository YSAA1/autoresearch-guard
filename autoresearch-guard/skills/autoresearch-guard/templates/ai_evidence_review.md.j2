# AI 证据审查

## 证据摘要

TBD by AI（在运行 `arx_audit.py` 后填写）。

## 结论与证据

每个 claim 必须使用最弱可支持等级：`exploratory` < `supported` < `verified`。
`状态` 只能表达 `supported`、`unsupported` 或 `prohibited`。
`verified` 的证据列必须是可解析路径或 http(s) URL，且 `research.yaml` 中对应 adversary 为 `survived`。

| claim_id | 结论 | 等级 | 证据 | 状态 |
| --- | --- | --- | --- | --- |

## 失败归因

TBD by AI（待填写）。请勿让脚本填写本节。

## 下一步动作

TBD by AI（待 AI 与人工门禁确认后填写）。
