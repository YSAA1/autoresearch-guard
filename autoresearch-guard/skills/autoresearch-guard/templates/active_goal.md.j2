# AutoResearch Guard Goal

迭代：{{ iteration_id }}

## 目标

{{ objective }}

## 假设

{{ hypothesis }}

## 协议

- 协议文件：`.research/current/protocol.lock.yaml`
- 协议摘要：`{{ protocol_digest }}`
- 允许的划分：{{ allowed_splits }}
- 禁止的划分：{{ forbidden_splits }}

## 允许的工作

{{ allowed_work_lines }}

## 禁止的工作

{{ forbidden_work_lines }}

## 被拦截动作

{{ blocked_action_lines }}

## 论断边界

- 最高论断等级：{{ max_claim_level }}
- 禁止的论断：{{ forbidden_claims }}

## 结束前必须产出

{{ must_produce_lines }}

## 确定性门禁

{{ validation_gate_lines }}

## 循环预算

{{ loop_budget_lines }}

## 运行约定

1. 这是研究 campaign 的单次 iteration，不是宿主内部 agent loop。先运行 `arx_loop.py start --research-root .research`；启用 hooks 时，PreToolUse 会把当前 Codex session 绑定为 owner。
2. 一次只做一个有界实验。每次实验后用稳定的 `attempt_id` 调用 `arx_record.py`，再读取 ledger 和门禁结果决定下一步。
3. 每次准备结束前运行 `arx_loop.py check --research-root .research --json`，把完整输出放进当前对话，让 Goal evaluator 能看到确定性证据。
4. `outcome: achieved` 才表示闭环完成。`blocked_requires_human`、`budget_exhausted`、`no_progress`、`aborted` 和基础设施错误都是受控停止，不得伪装成完成，也不得继续盲试。
5. Stop hook 只提供一次补救 continuation；它不是循环引擎。重入、等待人工、预算耗尽或无进展时必须允许/强制当前 turn 结束。

## 完成规则

在 evidence 已记录、最终 audit 绑定最新 review/ledger、decision 已提交且 `arx_loop.py check --json` 返回 `ready: true` 之前，不得把目标报告为完成。若返回受控停止 outcome，应留下原因、预算和下一条人工可执行动作后停止。
