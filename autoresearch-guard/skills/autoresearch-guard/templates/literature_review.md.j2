# Literature Review

迭代：{{ iteration_id }}

## 候选创新点

每个候选必须附证据链接（S2 paperId 或 arXiv ID）。

TBD by AI（至少 1 条，格式：idea_id | 描述 | 证据链接 | gap）。

## 现有实现（不重复造轮子）

每个候选方法的已有仓库/代码。复用优先，重写需在 hypothesis.yaml 给 build_new_reason。

TBD by AI（至少 1 条，格式：name | url | covered_capability）。

## 引用图谱路径

从 seminal work → 后续工作 → 未覆盖 gap 的路径。用 S2 references / citations 工具遍历。

TBD by AI。

## Gap 分析

现有工作 + 现有代码未覆盖之处 = 创新点来源与复用边界。

TBD by AI。

## 推荐所用 seed 论文

调用 `semantic_scholar_recommendations` 用的 seed paperId 列表。

TBD by AI。
