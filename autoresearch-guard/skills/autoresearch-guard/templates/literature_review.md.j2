# Literature Review

迭代：{{ iteration_id }}

## 候选创新点

每个候选必须附证据链接（S2 paperId 或 arXiv ID）。`hypothesis.yaml.evidence_basis` 必须引用这里的 `idea_id`。

| idea_id | 描述 | 证据链接 | gap |
| --- | --- | --- | --- |

## 现有实现（不重复造轮子）

每个候选方法的已有仓库/代码。复用优先；`hypothesis.yaml.reuse_plan.base` 必须引用这里的 `impl_id` 或 `url`。若 `base: build_new`，必须在 hypothesis.yaml 给 `build_new_reason`。

| impl_id | url | covered_capability | reuse_decision |
| --- | --- | --- | --- |

## 引用图谱路径

从 seminal work → 后续工作 → 未覆盖 gap 的路径。用 S2 references / citations 工具遍历。

TBD by AI。

## Gap 分析

现有工作 + 现有代码未覆盖之处 = 创新点来源与复用边界。

TBD by AI。

## 推荐所用 seed 论文

调用 `semantic_scholar_recommendations` 用的 seed paperId 列表。

TBD by AI。
